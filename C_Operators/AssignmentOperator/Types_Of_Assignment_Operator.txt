ASSIGNMENT OPERATORS IN JAVA:
->assignment operators are used to assign values to variables. 
->The most common assignment operator is the equal sign (=),which assigns the value on the right to the variable on the left.
->There are also compound assignment operators that combine an arithmetic operation with assignment, such as +=,-=,*=,/=,%=.
->The basic assignment operator (=) assigns the value on the right to the variable on the left.

For example:
int a = 10; // assigns the value 10 to the variable a   
int b = 20; // assigns the value 20 to the variable b
->The compound assignment operators perform an arithmetic operation and then assign the result to the variable.

For example:
a += 5; // equivalent to a = a + 5; now a is 15
b -= 10; // equivalent to b = b - 10; now b is 10
a *= 2; // equivalent to a = a * 2; now a is 30
b /= 5; // equivalent to b = b / 5; now b is 2
a %= 4; // equivalent to a = a % 4; now a is 2

#TYPES OF ASSIGNMENT OPERATOR:

1) Basic Assignment Operator (=) :
->The basic assignment operator (=) is used to assign a value to a variable.
->It takes the value on the right-hand side and assigns it to the variable on the left-hand side.

2) Compound Assignment Operators:
->Compound assignment operators combine an arithmetic operation with assignment.
->They perform the specified arithmetic operation on the variable and then assign the result back to that variable.
->The compound assignment operators include: +=, -=, *=, /=, %=.

(I) Addition Assignment Operator (+=) :
->The addition assignment operator (+=) adds the value on the right-hand side to the variable on the left-hand
  side and assigns the result back to that variable

(II) Subtraction Assignment Operator (-=) :
->The subtraction assignment operator (-=) subtracts the value on the right-hand side from the variable on the
  left-hand side and assigns the result back to that variable.

(III) Multiplication Assignment Operator (*=) :
->The multiplication assignment operator (*=) multiplies the variable on the left-hand side by the value on the 
  right-hand side and assigns the result back to that variable.

(IV) Division Assignment Operator (/=) :
->The division assignment operator (/=) divides the variable on the left-hand side by the value on the right-hand 
  side and assigns the result back to that variable. 

(V) Modulus Assignment Operator (%=) :
->The modulus assignment operator (%=) takes the modulus of the variable on the left-hand side by the value on the
  right-hand side and assigns the result back to that variable.



NOTE:
(1) WE USE THIS OPERATOR ONLY BECAUSE OF FASTER EXECUTION AND LESS CODE TO WRITE.
(2) ALL THE COMPOUND ASSIGNMENT OPERATORS CAN BE USED WITH ANY DATA TYPE THAT SUPPORTS THE CORRESPONDING ARITHMETIC OPERATION.
(3) THE BASIC ASSIGNMENT OPERATOR (=) CAN BE USED TO ASSIGN VALUES OF ANY DATA TYPE TO VARIABLES OF THE SAME OR COMPATIBLE DATA TYPE.   